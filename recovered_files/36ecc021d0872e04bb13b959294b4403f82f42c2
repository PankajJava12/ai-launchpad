
Build the app only
docker-compose -f docker-compose-aws.yml build --no-cache rag-app


AWS cli logs

(base) pankajdewangan@Pankajs-iMac RAG % VPC_ID=$(aws ec2 create-vpc \
  --cidr-block $VPC_CIDR \
  --tag-specifications 'ResourceType=vpc,Tags=[{Key=Name,Value=rag-vpc}]' \
  --region $AWS_REGION \
  --query 'Vpc.VpcId' \
  --output text)

echo "VPC ID: $VPC_ID"
VPC ID: vpc-03586f3ae1f1fee54


